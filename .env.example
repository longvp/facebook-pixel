# Copy to .env and fill in real values.

# MySQL connection for Prisma.
DATABASE_URL="mysql://root:PASSWORD@127.0.0.1:3306/facebook_pixel_scaffold"

# 32-byte base64 key for encrypting CAPI access tokens at rest.
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
APP_ENCRYPTION_KEY=""
